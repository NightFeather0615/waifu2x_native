name: waifu2x_native
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  rust_lib_waifu2x_native:
    path: rust_builder
  flutter_rust_bridge: 2.5.0
  texture_rgba_renderer: ^0.0.16
  path_provider: ^2.1.4
  file_picker: ^8.1.3
  freezed_annotation: ^2.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0
  integration_test:
    sdk: flutter
  build_runner: ^2.4.13
  freezed: ^2.5.7

flutter:
  uses-material-design: true
